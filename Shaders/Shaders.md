
With OpenGL shaders are written in [[GLSL (OpenGL Shading Language)]]. These shaders are part of the rendering pipeline and used to process raw data to actually render something on the screen.

![[pipeline.png]]
The most common and shaders are the [[Vertex Shader]] and [[Fragment Shader]] shaders used to set vertices and color respectively.
